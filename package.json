{
  "scripts": {
    "start": "node backend/dist/main/index.js",
    "build-frontend": "cd frontend && npm install && npm run build && cd ..", 
    "build-backend": "cd backend && npm install && npm run build && cd ..",
    "heroku-postbuild": "npm run build-frontend && npm run build-backend"
  }
}
